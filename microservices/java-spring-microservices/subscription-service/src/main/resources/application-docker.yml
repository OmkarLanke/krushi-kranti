server:
  port: 4013

spring:
  application:
    name: subscription-service
  
  datasource:
    url: jdbc:postgresql://subscription-db:5432/subscription_db
    username: postgres
    password: ....
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

# gRPC Server Configuration
grpc:
  server:
    port: 9093

# gRPC Client Configuration (to call Farmer Service for validation)
  client:
    farmer-service:
      address: static://farmer-service:4000
      negotiationType: plaintext

# Subscription Configuration
subscription:
  amount: 999
  currency: INR
  validity-days: 365
  trial-days: 0

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.krushikranti.subscription: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

