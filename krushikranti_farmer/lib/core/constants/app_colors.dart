import 'package:flutter/material.dart';

class AppColors {
  // --- BRAND COLORS ---
  static const Color brandGreen = Color(0xFF2E7D32); // Dark Green
  static const Color primary = Color(0xFF2E7D32);    // Main Action Color
  static const Color accent = Color(0xFFFFD600);     // Yellow/Amber

  // --- UI COLORS ---
  static const Color background = Color(0xFFFAFAFA); // App Background
  static const Color surface = Colors.white;         
  static const Color cardSurface = Colors.white;     
  
  // --- DASHBOARD SPECIFIC (Your Design) ---
  static const Color creamBackground = Color(0xFFFFF8E1); // Weather Card
  static const Color pendingStatus = Color(0xFFFF7043);   // "Pending" Text
  static const Color alertText = Color(0xFFC62828);       // "Alert" Text
  static const Color lightGreenCircle = Color(0xFFE8F5E9); // Icon Background
  
  // --- TEXT COLORS ---
  static const Color textPrimary = Color(0xFF1B1B1B);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnButton = Colors.white; 
  
  // --- UTILITY COLORS ---
  static const Color border = Color(0xFFEEEEEE);
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
}